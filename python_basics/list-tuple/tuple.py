# Tuples are immutable, which means the data is read only.
# Decalre a tuple by varible = (DATA, DATA, DATA)
# Tuples can be unpacked (extrapolated) into other variables.

tupletounpack = ("Cassius", "Cyber Security Management")

name, course = tupletounpack


